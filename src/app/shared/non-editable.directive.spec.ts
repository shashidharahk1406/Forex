import { NonEditableDirective } from './non-editable.directive';

describe('NonEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new NonEditableDirective();
    expect(directive).toBeTruthy();
  });
});
