import { FormSpaceDirective } from './form-space.directive';

describe('FormSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
